# datasets/

Local storage for raw, processed, and sample datasets used during development and testing.

Large or generated files should not be committed to version control. Use subdirectories such as `raw/` and `sample/` as the project grows.

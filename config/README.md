# config/

Non-secret application configuration: YAML/JSON defaults for the simulator, zone definitions, logging, and future service settings.

Secrets and environment-specific values belong in `.env` at the project root (never committed).
